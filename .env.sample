# Supabase Project URL - Find this in your Supabase project settings under API
SUPABASE_URL="https://your-project-ref.supabase.co"

# Supabase Public Anonymous Key - Find this in your Supabase project settings under API
SUPABASE_ANON_KEY="your-supabase-anon-key"

# In a real production environment, you would use a secure way to handle this,
# possibly through Vercel/Coolify environment variable settings.
